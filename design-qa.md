# Jev Moral Railway — ブラウザ・デザイン検証

2026-09-19 / Codex内ブラウザ / React + Vite / http://127.0.0.1:5173/

結果: **デモは実操作で完走・再プレイまで確認済み**。実APIはキー未設定につき未接続。

## 選定画像との比較

基準: `design-reference.png`（1487×1024）。同じ1487×1024 CSS viewport、DPR1の初期画面 `qa/01-initial.png` と併せて目視比較。途中画像では題名がトロッコ、吹き出しが顔を隠したため、背景の位置・マスコットの収まりを調整して再撮影。

維持した要素: ネイビーの番組風ステージ、シアン／赤の左右選択、キツネ車掌、左の線路と右の判断パネル、中央の問い。
修正: 題名のシアン枠・太字の選択肢、設定説明の折り畳み、狭幅の札の折り返し、結果へのフォーカス移動と直後の次問操作。

相違: 参照画像とピクセル単位では一致しない。写真内の装飾文字・ロゴ・ボタン形状の完全再現はしていない。架空参加人数と架空の判断理由は合意に従い表示しない。結果は確定まで隠し、初期画面にモードと架空データの明示を加えた。背景は各問共通の舞台イメージであり、問題文の人数を優先する旨を表示。

## 操作の確認

|順序|操作・状態|結果|画像（ローカルQA）|
|---|---|---|---|
|1|初期画面|デモ、未選択、0/3、確定ボタン無効、APIキーなしでAPI選択不可|01-initial.png|
|2|2択・選び直し|左右を選択でき、確定前はJevの判定非表示|02-choice.png|
|3|確定|入力ロック、結果へフォーカス、8/92・確信度60、一致表示、1/3|03-reveal.png|
|4|次問|2問目に切替、選択解除、題名へフォーカス、ページ先頭へ移動|04-next-mobile.png|
|5|狭幅の判定・説明|320pxで札の重なりなし。2問目は不一致。架空の値と61/39・確信度4、説明の開閉、次問操作|05-mobile-reveal.png|
|6|3問終了|3問目44/56・確信度1、3/3回答後に結果画面、一致回数2/3|06-finish.png|
|7|再プレイ|1問目へ復帰、0/3、未選択・判定非表示、デモ、モード選択可能|07-replay-mobile.png|

390×844と320×844のブラウザ幅で確認。横方向overflowなし、320pxの2問目で左右札が重ならないことをDOM矩形でも検証。修正前は札の非重複と結果フォーカスの2検査がfalse、修正後はtrue。判定確定をEnterでも操作し、見出し/結果へのフォーカス移動を確認。console error/warnは0件。

## 検証とレビュー

- `npm ci`: 65 packages取得成功。新フォルダには旧node_modules/dist/.gitを移行していない。
- `npm test`: 17/17。端数丸めで確率合計101となる回帰テストを先に失敗させ、100となる修正後に成功。
- `npm run build`: 成功。`npm run test:sites`: 4/4。配信形式の検査であり、公開はしていない。
- セキュリティ検査: 架空のキーをプロセス環境へ渡して一時ビルドし、全出力から値が不在であることを確認。実設定は空欄、権限600、Git除外。開発サーバーの`/.env.local`、`/.env.local?raw`、`/.git/config`は403。`/api/status`はconfigured=falseだけ返す。
- Claude sonnet/lowへの読み取り独立レビューは認証エラーで未実施。利用上限と取り違えず、自動再試行・認証変更は行わなかった。主担当が自己レビューし、ゲーム進行、API境界、丸め、秘密値・個人パス、移行元の保持、参照整合性を検査。
- UI不具合は実ブラウザの修正前／後で回帰確認。文書変更は振る舞いの追加ではないためTDD対象外で、参照と最新の検証状態を照合。

## 証拠と制限

スクリーンショットとDOM検査結果はローカルの`qa/`に保存し、Agents Vaultの同日作業記録へ複製（qaはGit対象外）。このQAは実機スマートフォン検証や完全なアクセシビリティ適合検査ではない。

一部のfullPageキャプチャには実viewportと一致しない一時的なレイアウトが写ったため棄却。採用画像はviewportスクリーンショットを保存後に再読込して確認した。以前の接続・起動制約は今回の環境で再現せず。TypeSafe実認証・残高・課金、公開用の認証／利用量制限は未確認・未実装。


## 2026-09-19 — Play-only Jev運行への変更

final result: pass

### 比較対象と変更理由

- Source: `design-reference.png`、実寸1487×1058。以前の1487×1024という記述は表示用viewportとの混同で、こちらが画像の実寸。
- Rendered: Chrome、1487×1024 CSS px / DPR 1、390×844、320×844。
- 参照と実装を同じ比較入力で開いて確認。ready同士で比較し、モックの最初から表示済みの確率・理由は実データ未取得のため空欄にするという状態差を考慮した。両画像のnative幅は同じ、縦34pxはモックの末尾装飾の差でありレイアウトの不具合としない。
- ユーザーが「プレイを押すだけで、Jevの選択とトロッコの動きを見る」と確認。人間の選択ボタン・一致回数は廃止し、2つの進路カードと3問のJev履歴に置換。ネオン番組・シアン/赤・狐車掌・大きな鉄道・右の判断パネルを保持。

### 見つけた問題と修正後の証拠

| 重要度 | 問題 | 修正・再確認 |
| --- | --- | --- |
| P2 | モバイルの判定モードが舞台より下で見つけにくい | Playの直前へ移動。320pxでもラベル・選択欄・ボタンが読める |
| P2 | デスクトップで舞台が高く、両進路カードが初期画面外 | バナーと操作部を詰め、舞台を2.5:1に。`qa/autoplay-12-final-ready.png` で両カードが同時に見える |
| P2 | 縦比率の変更で車両の軌道が背景の線路からずれる | 背景幅を基準にした座標へ統一。`qa/autoplay-13-final-route.png` で右分岐へ着地、左方向も実操作 |
| P2 | 320pxの長い説明札が左へ移動した車両に重なる | 札を舞台の下側へ。`qa/autoplay-15-narrow-final.png` とDOMの矩形で重なりなし、横スクロールなし |

最終の待機位置を車輪の透明余白に合わせて微調整。`qa/autoplay-16-ready.png` が最終ready、以前の比較は保持。受け入れ可能な差分: 操作と問題文を絵の上へ明確に分離、実データ前の空欄、正確な人数を示す文章と固定人形の除去、実際の生成理由がないため理由欄を作らない。

### 必須の5観点

- Typography: 既存のArial / Hiragino / Yu Gothicと太い見出しを維持。大きな問い、進路、補足の順の階層を確認。320pxでは長い日本語が折り返し、切れない。
- Spacing/layout: 上の問題・操作、中央の舞台、下の2進路、右の判断という構成。幅901px以上で密度を修正。狭幅は縦配置、Playは初期画面内。境界と左右の説明札に重なりなし。
- Colors/tokens: navy #050817、cyan #1ee4ff、red #ff4168、goldの状態表示。モードと架空データの説明は文言でも区別。
- Image quality: 元画像を保存し、imagegenの空背景と実透過車両を使用。狐は既存画像。拡大・軌道を画面で検証、車両をCSS図形で代替していない。
- Copy/content: Playが自動判断開始を表し、デモは架空と明記。選択確率・確信度は正解率ではない。本人が生成していない理由を表示しない。

native幅の比較で文字・部品を判読でき、追加の拡大切り抜きは不要。狭幅は専用の実寸キャプチャを使用した。モックにないloading/moving/historyは独立した操作検証を実施。

### 検証範囲

- ready→Play→moving→arrived、左右の進路、運行中の操作無効化、次問で見出しfocus/scroll0、3問の履歴、再プレイで0/3・demoに戻ることをブラウザで実操作。
- modeをliveに切り替えてもreadyのまま、通信ボタンの説明が明確であることを確認し、Playは押さずdemoへ戻した。実API呼び出しは未実施。
- 18単体テスト、ビルド、4配信形式テスト成功。状態遷移は先に失敗するテストを確認後に実装。
- `prefers-reduced-motion` は移動を省略して最終位置を表示する実装をコードレビュー。OS設定切替での実画面確認は行っていない。
- 秘密ファイルは未読。Git除外とindexのパスを確認。架空sentinelでビルドし、ブラウザ配信物に値・envファイルが含まれないことを検証。
- Claude sonnet/lowレビューは認証エラーで未実施（利用上限ではない）。主担当の自己レビューで上記P2を修正し、状態遷移・再送・モード固定・タイマー解除・秘密境界・差分を確認。公開/pushなし。

### 生成素材（built-in image_gen）

- `public/assets/railway-stage-empty.png`: 元の背景からトロッコと人物だけを取り除き、線路・カメラ・ネオン・風景を保持する編集。
- `public/assets/trolley-sprite.png`: 元の正面向きトロッコを、tealと真鍮・暖色前照灯・cyan/red反射を保った実透過PNGにする編集。
- 完全なプロンプトと出力情報: ローカル `qa/railway-stage-generation.md`、`qa/trolley-sprite-generation.md`。Vaultの今回の作業記録にも保存。


## 2026-09-19 — Jevの運転席視点（過去。走行方式は後節で更新）

final result: pass

ユーザーが「運転手視点なのに外観のトロッコが映っている」「Jevを運転手として、回答後に前方の人へ進む」と訂正。前節の車両スプライト構図は撤回し、first-personの景色＋人数＋固定コックピットへ変更。

### 視覚比較

- `design-reference.png` (1487×1058) と `qa/pov-01-ready.png` (1487×1024 / DPR1) を同じ比較入力で開いた。視点・衝突までの演出は新しいユーザー指示が優先。旧モックに忠実な外観車両や側面プラットフォームは復元しない。
- Fonts/typography: 太い問いと進路、補足の階層を維持。長いタイトルの折り返し、数字を320pxでも確認。
- Spacing/layout: 左にプレイ・運転席・2進路、右にJevのパネル。狭幅は上下配置。人物群が操作盤や人数札に隠れないことを初期画面で確認。
- Colors/tokens: 選定済みnavy/cyan/redとgoldの結果表示を維持。白い人物は左右照明で背景から分離。
- Image quality: 新背景1942×809、実透過コックピット1942×809、人物1024×1536。CSSは透明余白を切り取り、人体を図形で描いていない。景色の接近は拡大・水平移動で表現するため終点では背景が柔らかくなるが、短い演出の意図した差。運転席は固定、外観トロッコはDOMに0件。
- Copy/content: Jevを運転手と明記。人数はシナリオと一致。結果は「1人／4人／研究者1人が巻き込まれました」。デモを架空と表示し、倫理的な正解や創作のJev理由は提示しない。

初回POV比較に視覚のP0/P1/P2なし。全幅画像で文字と部品を読め、追加の拡大切り抜きは不要。狭幅は `qa/pov-05-mobile-ready.png` (390px)、`qa/pov-06-narrow.png` (320px) を実寸で確認。横あふれなし。

### 振る舞いとレビュー

- 新テストでimpact未実装・人数未設定を先に失敗させ、実装して通した。人数を挿入する一時処理の誤りもテストが検出し、全3問を修正。
- ブラウザで第1問5/1→右へ接近→impact（暗転のopacity上昇、Play無効）→1人の結果、第2問4/1→左→4人の結果、第3問2/1→右→研究者1人の結果を確認。
- `qa/pov-02-approach.png`、`qa/pov-03-impact.png`、`qa/pov-04-consequence.png`、`qa/pov-07-summary.png` を保存。再プレイで5/1・0/3・demo・ready・見出しfocus・scroll0。
- 自己レビューでimpact中の進行メッセージに旧ready文言が残る問題を修正し、ブラウザで到達メッセージを再確認。reduced-motionの暗転抑止もCSS優先度を修正。OS設定での実画面検証は未実施。
- Browser toolの短い状態待ちがdeadline exceededになる場面があり、その試行でimpactを確認したとは扱わない。第1問で実際のimpact状態・disabled・curtain opacityを取得できた証拠を採用。各試行後の新しい画面で最終状態を確認し、重複してPlayを押していない。
- 20単体テスト・ビルド・4配信形式テスト成功。API中継や秘密ファイルの設定は変更なし。実API呼出・残高確認・実機確認は未実施。

### 素材とプロンプト（built-in image_gen）

- `public/assets/railway-driver-view.png`: 下中央から左右へ分岐する運転者視点。既存のネオン・山・街の方向性を保持し、車両・人物・操作盤は含めない。生成記録 `qa/driver-view-generation.md`。
- `public/assets/railway-person.png`: 中立で無傷の白い単一ゲーム駒、左cyan/右redの照明、実透過。生成記録 `qa/railway-person-generation.md`。
- `public/assets/jev-cockpit-overlay.png`: 前方80%以上が透明な操作盤とキツネの手元のみ。生成記録 `qa/cockpit-generation.md`。

完全なプロンプト・透過確認・生成ツール情報は上記ローカル記録とVaultに保存。元画像は削除していない。

今回のPOV差分の別実行レビューもClaude sonnet/lowで依頼したが、run `20260919T075059-457d53963ed9` は認証エラー。利用上限ではない。主担当が差分・影響範囲を自己レビューし、上記メッセージ/暗転抑止の修正を採用。秘密ファイル未読・配信境界維持を確認。

## 2026-09-19 — 立体線路・可動分岐器・連続運行（過去。後節で接続・終点を更新）

Final result: pass for local demo. Actual TypeSafe API remains unverified.

### 要求と選定

利用者の指示: 回答 → 可変レール切替 → 人への衝突 → 次の分岐と質問 → 繰り返し。画像の拡大・横移動を撤回し、Three.jsの線路と運転席カメラを共通の距離パスに置き換えた。前の「PlayだけでJevが判断する」は維持。「次の問いへ」ボタンは廃止。次の質問が表示されても、次のPlayまでは判断/APIを実行しない。

### 視覚QA

選定画像 `design-reference.png`（1487×1058）と `qa/rail-14-final-ready.png`（1487×1024）を同じ入力で目視比較。ユーザーの新しい運転席・線路要件を優先し、旧外観車両、壇上の人、観客は再現対象から外した。受け入れる差分: 運転席の大きい舞台、明示的なモード/Play、結果未取得時の空欄、判断理由を創作しないこと。

- Typography: 太い日本語の問い、進路カード、細い補足の階層を保持。320pxで長い日本語が折り返す。
- Spacing/layout: 左の問い/運転席、右のJevパネルを保持。狭幅は縦配置。390px・320pxとも横幅とscrollWidthが一致。全体スクロールでカードと判定も読める。
- Colors: navy/cyan/redを維持。可動部はgold、選択された進路と確率は従来の色と文章で示す。
- Image quality: skyline、人物、Jevの手元はimage_genの画像。線路・枕木・ポイントは走行のための3D形状。カメラも線路中心の経路を使う。画像拡大による走行はない。人物は前後で隠れない1列に修正。
- Copy: 初期デモ、架空固定値/架空投票、正解率ではない説明を保持。衝突の結果は人数/条件を含む。外観トロッコは表示しない。

### 問題・修正・検証

| 問題 | 修正と根拠 |
| --- | --- |
| 旧方式では線路と移動方向がずれる | 線路/カメラが同じcenterline。距離で進め、加速・減速する。位置・向きの接続を両経路でテスト |
| 分岐器が動かない | 固定のstock railに加え、heelを固定してtoeが動く金属ブレードと連動棒を実装。固定完了前は走行しない |
| 次の問いが手動切替 | 衝突後に接続された次タイルを出し、走行終了時に問いを自動更新。次のAPIは自動実行しない |
| カーブ中に背景の右端が見える | 平面背景を広い円筒内面に変更。`qa/rail-08-curve-fixed.png`で端・段差なしを再確認 |
| 後列の人が前列に隠れる | 全員を横1列に。`qa/rail-14-final-ready.png`で5人を判別 |
| 停止時もGPU描画、描画中断時も進行 | 停止時は変更がある時だけ描画。context lossは進行停止＋再読込案内。再プレイ時は描画資源を解放 |

ブラウザ実操作: 第1問は分岐器switch固定→支線curve→impact時next=hidden→departing時next=visible→第2問ready。第2問はstay固定→直進→4人の結果→第3問ready。第3問は390pxで支線→研究者1人の結果→3判断の履歴へ自動遷移。再プレイはdemo/ready/0問/第1問/H1 focusへ戻り、再描画完了後Playが有効。

証拠: `qa/rail-02-switching.png`, `rail-08-curve-fixed.png`, `rail-09-straight.png`, `rail-07-impact.png`, `rail-10-mobile.png`, `rail-11-mobile-moving.png`, `rail-12-summary.png`, `rail-13-320.png`, `rail-14-final-ready.png`。途中の旧平面背景・人物配置の画像も履歴として保持。初回の短いselector待ちが期限切れになった試行は証拠に数えず、区間ごとの状態待ちで上記一連を取り直した。

### テスト・レビュー・秘密境界

- 分岐固定と連続パスの新テストを先に実行し、未実装のexport/moduleで失敗することを確認後に実装。26テスト成功、配信形式4テスト成功、ビルド成功。
- JSは757.18kB / gzip204.01kB。Viteの500kB警告は残るが、3D実装の配信サイズとして記録。描画比率を1.5に制限、枕木はインスタンス化。
- 別実行のClaudeレビューは既記録の認証エラーのため未実施。認証を無断変更せず、主担当が自己レビュー。新しい状態遷移、タイミング、境界座標、再プレイ破棄、エラー、既存API/秘密境界、差分を点検。指摘は上記へ反映。
- 実APIは一度も呼ばず、設定確認はローカル/api/statusのconfigured=trueのみ。秘密envは未読。Gitに追跡されたenvは空テンプレートだけ。
- 架空sentinelをプロセス環境に渡して本番ビルドし、ブラウザ成果物にsentinel/環境ファイル/TypeSafe呼出先が含まれないことを確認。実キーの読取りや値比較はしていない。
- 最新のブラウザ確認で新しいconsole error/warnは取得されず、ログには旧07:25のHMR失敗2件のみが残っていた。
- reduced-motionは静止表示へ切り替える実装をレビュー。OS設定での実画面確認、WebGL非対応端末、物理スマホ、実API認証/残高は未検証。
- 公開、push、デプロイなし。

生成した3D用背景: `public/assets/railway-skyline.png` 1942×809。線路・車両・人物なし、下側は低詳細の濃紺霧。built-in image_gen、全プロンプトは `qa/skyline-generation.md` とVaultに保存。既存の素材を削除していない。

## 2026-09-19 — 途切れない線路・終点駅・助ける相手の問い（過去。後節で選択と走行を更新）

Final result: pass for local demo. Actual TypeSafe API remains unverified.

### 要求・設計・実装

利用者から「レールをつなぐ」「3問後は終点駅へ」「デモも本番と同じ倫理的な問い」「選択肢は線路の方向ではなく科学者1名／ニート5名のような相手」と追加指示。Product Designのimage-to-codeを継続適用し、DESIGN.mdに選定理由を記録してから実装。

- 全分岐・再合流・駅までの線路を最初から作成。各支線は人を通過後に本線へ滑らかに戻る。次の人たちと問いだけを衝突後に表示する。
- 選択は助ける相手（left/right）、走行は犠牲になる反対側（stay/switch）へ変換。画面・デモ・APIに共通の対応関数を使用する。
- 3問は将来の貢献と人数、家族と他人、事故の責任と人数の対立。デモとAPIへ送る問題文・選択肢は同一。創作の理由・正解判定は表示しない。
- 最後の線路は終点駅へ接続。両側ホーム・屋根・照明・車止めを追加。6.2秒の駅進入後、車止め手前で停車してから「結果を見る」を出す。駅への移動と結果表示にAPI呼出はない。

### 視覚比較と修正

選定済み `design-reference.png`（1487×1058）と `qa/terminus-12-final-ready.png`（1487×1024）を目視比較。旧モックの車両外観や進路文言は新しいユーザー要件で置き換え、色・番組風の構成・Jevを維持。

- Typography: 問いを最も大きく、2択を次の階層に。320pxでも長い問題文と人物名が折り返し、欠けずに読める。
- Spacing/layout: 左に問い・Play・運転席・2択、右に判定パネル。デスクトップのシーン比率を2.6に調整し、2択が初期画面の下端に収まるよう修正。狭幅は縦配置。
- Colors: navy/cyan/red、可動レールと結果はgoldを保持。救助側のカードを明示し、進路の表示とは区別する。
- Images: 終点駅は新しい透過PNG。キャプチャで時計塔・終点の看板・両ホーム・車止めが読める。列車外観は置かずJevの手元を固定。3D線路とカメラが同じ中心線を使用する。
- Content: 「どちらを助ける？」と選択肢の人物群を表示。衝突後・結果画面で救助と犠牲の双方を記載。質問はAPIと共通、デモの判定と数値・投票だけが架空と明記。

現行画面に視覚のP0/P1/P2なし。390pxの終点・結果、320pxの初期・走行・次問で横幅とscrollWidthが一致。スクリーンショット全幅で判読でき、追加の拡大切り抜きは不要。

### ブラウザ実操作

Chrome / http://127.0.0.1:5173/ で確認。

1. 第1問: 科学者1名を助ける → 支線へ切替 → ニート5名が犠牲 → 再合流した線路から第2問。
2. 第2問: 見知らぬ4名を助ける → 直進 → 家族1名が犠牲 → 次の本線・第3問。
3. 第3問: 事故を起こした5名を助ける → 支線 → 通行人1名が犠牲 → 終点駅へ進入。進入中は結果ボタンなし。
4. 終点に停車（data-station=arrived）→「結果を見る」→3問の救助・犠牲・数値の履歴。
5. 390pxで終点・結果を確認。再プレイでデモ・第1問・0/3に戻る。320pxでも第1問をプレイし、第2問へ進行。最終タブはデモ初期画面へ戻した。

証拠は `qa/terminus-02-save-left-travel-right.png`、`terminus-03-connected-after-impact.png`、`terminus-04-next-trunk.png`、`terminus-05-arrival.png`（実際は駅手前のdeparting）、`terminus-06-platform.png`、`terminus-07-stopped.png`、`terminus-08-mobile-station.png`、`terminus-09-mobile-results.png`、`terminus-10-320-ready.png`、`terminus-11-320-moving.png`、`terminus-12-final-ready.png`。

再プレイのクリックでブラウザツールがタイムアウトしたが、直後の新しいDOMで既に第1問へ戻ったと確認したため再クリックしなかった。アプリのconsole error/warnは取得範囲で0件。

### テスト・レビュー・秘密境界

- 最初に新しい保存先/分岐/駅のテストを実行し、未実装export・旧choiceキー・旧人数で11件失敗。実装後31単体テスト成功。配信形式4テストと本番ビルド成功。
- 両側の救助→反対方向、各分岐の位置/接線接続、支線の再合流、駅線路の接続、停車前に結果へ進めないことを確認。
- 主担当が差分を自己レビュー。状態遷移・重複イベント・人数・API契約・3D資源解放・狭幅・秘密境界を確認し、デスクトップで2択が下に出過ぎる点を修正。別実行のClaudeレビューは既記録の認証エラーが未解消のため今回も未実施。利用上限への読み替えや認証変更はしない。
- 架空sentinelを渡したビルドで、12個のクライアントファイルにsentinel・TypeSafeの呼出先・envファイルがないことを確認。Gitで追跡されるenvは空テンプレートだけ。秘密ファイルの中身は読んでいない。設定変更なし。
- JS760.49kB / gzip205.27kB、500kB警告は残る。ブラウザの狭幅検証であり物理スマートフォンは未検証。reduced-motionの実画面検証・実APIの認証/応答/残高は未実施。
- 公開・push・デプロイなし。

生成素材: `public/assets/terminal-station.png`（1774×887、実透過、built-in image_gen）。全プロンプト・出力情報は `qa/terminal-station-generation.md` とVaultに保存。


## 2026-09-20 — 犠牲になる側の選択・終点までの連続運行（最新）

Final result: passed for local demo and simulated delayed/error responses. Actual TypeSafe API remains unverified.

### 要求・選定・実装

ユーザーの訂正: 選ぶのは助けない方（轢く方）。質問の間には停止せず、全問終了後の駅だけで停止。応答時間は合流から次の分岐までの距離で確保する。Product Design 0.1.55 image-to-codeを継続適用、user-context preflight成功。選定済みネオン画面と運転席の構成を維持し、DESIGN.mdに変更方針を記録して実装した。新規画像なし。

- choice left/rightは選んだ犠牲側。left→本線、right→支線。同じ側へ走り、反対側が助かる。質問・2択・APIのhit質問・結果文・確率の説明を揃えた。
- Play1回で3問を連続判定する。初期デモ。実API選択だけでは開始しない。開始前に通常最大3回の呼出を明記し、再試行分の追加利用も表示する。
- 物理位置と現在の問いを分離。発車時のみ加速し、その後10m/sを保って問/線路の境界を通過。最後の駅に10m/sで入り、連続的に減速して0にする。
- 最初の2区間を256m、最後の分岐を96mにし、合流後に長い直線を追加。衝突後の結果表示を経て、走行中に次の問いを依頼する。返答未確定なら視界外で直線を160mずつ延長。通過済みの延長描画を解放する。
- 通信エラーは明示し、直線を走り続ける。自動再送・架空の代替回答・未決定の分岐への侵入なし。手動再試行は1回だけ依頼。同じ描画からの重複依頼はセッションで抑止。

### 視覚QA

`design-reference.png`（1487×1058）と `qa/nonstop-09-final-ready.png`（1487×1024）を同じ入力で開き目視比較。既に合意した一人称への置換と今回の選択文言を優先し、参考画像の外観車両・進路ボタン・架空の理由文は復元しない。

- Typography: 問い、人物群、補足の階層を保持。長い問いと犠牲/救助の説明は320pxでも欠けずに折り返す。
- Spacing/layout: 左に問い/Play/運転席/2択、右にJevパネル。狭幅では縦配置。390px・320pxのscrollWidthはviewport幅と一致。
- Color/image: navy/cyan/red、goldの可動部、既存の風景・人物・手元・駅を維持。連続線路と待機中に延長した直線で、途切れ・瞬間移動・画面内の分岐の移設は観測しない。
- Copy: 犠牲にする選択と救助された相手を区別。3問自動進行・実APIの呼出回数・エラー時の再試行を表示。デモ/投票の架空表示と倫理的正解率ではない説明を保持。
- 視覚P0/P1/P2なし。P3: デスクトップの第1問説明の末尾が短い2行目に折り返す。意味や操作を妨げず、今回の範囲では保持。

### ブラウザの実操作・証拠

Chrome / http://127.0.0.1:5173/ で、Playを1回だけ押して科学者1名→見知らぬ4名→事故を起こした5名を犠牲にする3問、駅への進入、停車、結果を確認。第2問の分岐進入はdistance=262.486m、speed=10.000m/s。駅進入中2.343m/s→到着後0.000m/s。「結果を見る」は到着後に出る。再プレイでdemo/第1問/readyへ戻り、Play有効。

実APIモードの選択だけではreadyを維持し、「開始すると3問を自動判定（最大3回・残高を使用）」を表示。Playは押さずデモへ戻した。

別のローカル検証ページ `qa/nonstop-fixture.html` で2問目に11秒の遅延と通信失敗を注入。外部APIは使わない。error時にdistance=303.007m、physicalJunction=1、question=2、speed=10.000m/s、延長線路を観測。その後478.162mでも10.000m/s。390pxで再試行→走行を継続→第3問→移設された駅に進入→0.000m/sで停車→3問の履歴を確認。検証ページはqa内のみ、Git/配信物に含めない。検証タブを閉じ、通常ゲームの初期画面を保持した。

証拠: `qa/nonstop-01-ready.png`、`nonstop-02-second-question.png`、`nonstop-03-third-or-station.png`（実際は駅へ減速中）、`nonstop-04-results.png`、`nonstop-05-error-cruising.png`、`nonstop-06-retry-mobile.png`、`nonstop-07-recovered-next-question.png`（実際は再試行後の駅進入）、`nonstop-08-320-ready.png`、`nonstop-09-final-ready.png`。console warn/errorは通常/模擬ページの取得範囲で0件。viewportを解除してデモ初期画面を引き渡し。

### TDD・レビュー・制限

- 先に選択の逆転、次問judging、自動依頼の重複抑止、連続速度、待機線路延長、駅への速度連続性のテストを失敗させてから実装。36単体テスト、4配信形式テスト、本番ビルド成功。
- 主担当が自己レビュー。選択とAPI契約、距離と接線の接続、初速/終速、遅延時の進路保護、重複/再試行/再プレイ、描画資源解放を確認。再試行後も「最大3回」のみを表示していた点を、追加利用の説明へ修正。浮動小数点の接線差は1e-9以内で検証し、位置の一致は厳密検証を維持。
- Claude sonnet/lowへ独立した選択/API部分を委譲したが、run `20260919T152051-0aa11af0d775` はauth_errorで未実行。actual_model=claude-sonnet-5、input/cache=0、output未報告。上限ではなく認証問題として扱い、主担当で続行。Claude独立レビューも未実施。
- 秘密env内容は未読。空.env.example以外はGit追跡なし。架空sentinelでビルドし、12配信ファイルにsentinel・TypeSafe呼出先・env・模擬ページがないことを検査。実API認証/残高/実応答は未検証、今回外部判定は0回。
- JS764.40kB/gzip206.59kB、既存の500kB警告あり。reduced-motionは静止したカメラ表示、背景タブはシミュレーション時間を止める既存の配慮を維持。物理スマホ/OS reduced-motion切替の実機確認は未実施。
- 公開/push/デプロイなし。

## 2026-09-20 — 短い区間・本番画面の整理（最新）

final result: passed

対象: Product Design 0.1.55 get-context / image-to-code / design-qaによる既存選定案の限定的な修正。user-context preflight成功。初期の方針をDESIGN.mdへ記録してから実装した。新規素材なし。

### 比較と修正

`design-reference.png` と `qa/compact-production-initial.png` を同一ツール入力で開いて比較。元案1487×1058、実装1487×1058相当（CSS viewport1487×1057、ブラウザ既存倍率によるDPR0.8。取得画像はCSS相当に正規化される）。元案の投票/操作/外観列車は、その後のユーザー訂正により置き換え済み。今回もこの意図した差を優先する。旧画面は `qa/compact-before.png`。主画面の読みやすいサイズで質問/Play/選択肢/右判定の各領域を比較できるため、別の拡大cropは不要だった。

- 字体・文字: 白い太字の問いと人物群、cyan/redの階層を維持。必要な倫理的前提は残し、英語の重複見出し、選択肢の重複説明、スローガンを削除。
- 配置・余白: 左の運転席と2択、右のJevを維持。投票・開発用キー設定パネルを削除。モードはヘッダの小さな開閉パネル。390pxでは人物と判定を並べ、不要な縦の余白を削減。
- 色・画像: navy/cyan/redと金の分岐器、同じ風景・人物・運転席・駅。近い未確定分岐の移動を隠すため遠景を空へ透過フェードさせる。最初の遅延検証では濃色の遠景シルエットが残ったため修正し、`qa/compact-delay-fade.png`で解消を確認。
- 内容: 架空の投票を廃止。「デモ · 架空の判定」は常時ヘッダ表示。確率・確信度は正解率ではない注記を1か所に集約。API選択時だけ呼出回数/残高の説明を表示。質問と画面構造は両モード共通。
- 自己レビュー修正: 第3問の通過後に「次の分岐へ」と表示していた箇所を「終点駅へ」に修正。衝突結果の読み上げを保持。未選択のカードの「選択肢」ラベルも削除。
- P0/P1/P2の未解決なし。P3: 長い第1問説明の末尾が短い最終行になる場合がある。意味や操作に影響しない。新規画像・装飾の追加は依頼範囲外。

### 動作・検証

- 通常の分岐区間256m→120m（元の96m+24m）。最終分岐は96m。待機延長160m→40m、未確定区間の残り20mでのみ延長。通常デモは走行44.04秒+最初の分岐器1.3秒、約45秒。
- TDD: 長さ上限と、衝突後の結果表示/0.8秒応答/分岐器切替を経ても延長なしで完走するテストを先に失敗させ、変更後に成功。37単体テスト・4配信テスト・本番ビルド成功。
- ChromeでデモのPlay、1→2→3問の自動進行、分岐器切替、衝突、終点への減速/0停止、3問の結果、再プレイを確認。第2問への境界122.331mで10.000m/s、駅進入2.779→到着0.000、余分な延長0。390pxで第3問の通過まで10.000m/sを確認。
- 模擬11秒遅延+エラー中も10.000m/sを維持し、未確定分岐には入らない。再試行後は終点へ到着して0.000。模擬ページはqa内のみ、外部APIを呼ばず、Git/本番配信物に含まれない。
- 開発表示は5173。本番用ビルドの表示は4174で起動。4173は別プロジェクト利用中のため変更せず4174を選択。開いた本番用ビルドでもPlay→第3問通過を確認。追加の最終結果は下記追記。
- 本番用静的プレビューに実API中継はない。5173の設定有無はconfigured:true、認証/残高/実応答は未確認。実APIモードの選択のみを試し、送信操作はデモでのみ実行。
- console warn/error取得範囲は0件。JS760.10kB/gzip205.41kB、既存の500kB警告あり。物理スマホ、OS reduced-motion設定の実操作は未検証。
- env秘密ファイルは未読。空テンプレートのみ追跡、env*除外。架空sentinel・TypeSafe endpoint・env・模擬ページの配信混入なし。ビルド内のTYPESAFE_API_KEYという識別子は既存の固定エラー案内で、秘密値ではない。

独立レビュー: native Codex gpt-5.6-luna / low、compact_review、読み取りのみ、指摘なし。Claudeは直前の記録済みauth_errorが未復旧（上限ではない）。既存アプリ内の独立レビュー機能を用い、別料金APIや認証変更なし。主担当で差分、文言、例外時走行、秘密除外を最終確認。

最終追記: 本番用ビルド http://127.0.0.1:4174/ で第3問の衝突後も10.000m/s、延長0を観測。その後終点で0.000となり、結果ボタンから3件の犠牲/救助/確率/確信度を表示、再プレイで第1問へ戻ることを実操作確認。`qa/compact-production-results.png`、`qa/compact-production-320.png`。CSS実幅320pxとscrollWidth320px、390pxとscrollWidth390pxで横はみ出しなし。ブラウザ制御の一部呼出にタイムアウトがあったが、直後の新しいDOM確認で既に完了した操作と未実行操作を区別して復旧した。アプリのconsole warn/errorは取得範囲0件。ブラウザの一時サイズ指定を解除し、本番ビルドの初期画面を引き渡す。公開・push・デプロイは行わない。


## 2026-09-20 — スタート画面・人質の前後関係（最新）

final result: passed

ユーザーの添付2枚を起点に、Product Design get-context / image-to-code / design-qa の限定修正を実施。事前の方針はDESIGN.mdへ記録。既存のネオン色・風景・Jev素材を再利用し、新規素材は作成していない。

### 表示と比較

- 初期画面と再プレイを、Jev・「トロッコ問題を遊ぶ」・短い導入・Playだけのタイトルへ変更。問題文、2択、人質、判定、問題番号を開始前には出さない。デモの架空表示と任意のモード開閉は維持。
- Play後に線路を初期化し、準備完了後に第1問の判定を開始。開始操作だけで従来の全3問から終点まで自動進行する。
- 透明な人物スプライトのdepthWriteを有効化。遠方の透明レールによる上書きを防ぎ、手前の実ジオメトリによる遮蔽と透過輪郭は維持する。
- 同一の第3問接近位置で修正前287.637m／修正後287.642mを撮影。`qa/start-occlusion-before.png` と `qa/start-occlusion-after.png` を同一入力で比較し、中央の人物を隠していたレールが消え、5人全員の顔と体が手前に描画されることを確認。検証用停止はqa内だけにあり、本体に停止フックを追加していない。
- `qa/start-production-desktop.png` は通常の本番ビルド。CSS実幅1487×1057、既存DPR0.8による取得画像の正規化1487×1058。白い太字、navy/cyan、元の人物・夕景を継承。遊び方の重複説明や問題の先出しなし。
- CSS実幅390px／320pxでscrollWidthも同値。モード開閉も右端に収まり、320px時のパネルleft40/right310。`start-production-390.png`、`start-production-320-menu.png`、`start-production-320-replay.png`。タイトルとPlayが読めて操作できる。モードパネルの基準位置を、問題番号がない初期画面でも崩れないよう修正。

### 実行・レビュー

- 実際のAppをReact SSRで描画する初期画面テストを先に失敗させた（`qa/start-tdd-red.log`）。全シナリオの本文と両群名が開始画面に存在しないことも検査。SSR試験はenvFile/configFileを無効化し、秘密値を読み込まず実行。
- 38テスト、4配信形式テスト、本番ビルド成功。既存の大きいJS警告あり（760.99kB/gzip205.66kB）。新しい依存なし。
- 本番ビルド http://127.0.0.1:4174/ でタイトル→Play→第1問→自動3問→終点（速度0.000）→3件の犠牲/救助/確率/確信度→再プレイでタイトルを実操作確認。狭幅320pxでも確認。通常アプリのconsole warn/errorは取得範囲0件。
- 独立レビュー: compact_review / Codex gpt-5.6-luna / low。Claudeの記録済みauth_error未復旧に対し、既存アプリの独立レビュー機能を利用。新しい料金経路や認証変更なし。指摘「初期描画失敗で待機から復帰不能」を採用して修正。WebGL初期化失敗のローカルfixtureで修正前を再現し、修正後の「スタート画面に戻る」操作を確認。対象限定の再レビューは指摘なし。
- 主担当が差分・変更範囲・初期化/再プレイ・秘密除外を自己レビュー。P0/P1/P2未解決なし。
- ブラウザ操作ツールの一部クリック応答がタイムアウトしたが、新しいDOMで操作完了を確認し、二重実行せず続行。アプリ側の異常なし。一時viewportを解除し、通常のスタート画面を開いて引き渡す。
- `.env*`秘密内容は未読。Git追跡は空`.env.example`だけ。12ブラウザ配信ファイルに架空ビルドsentinel、TypeSafe endpoint、env、qa fixtureの混入なし。既存のTYPESAFE_API_KEYという固定エラー文中の識別子は秘密値ではない。
- 今回の実API呼出0回、認証/残高/実応答は未確認。デモ動作確認と区別する。物理スマホ・OS reduced-motionの実機確認は未実施。公開/push/デプロイなし。


## 2026-09-20 — GitHubへの初回送信・本番ビルドの実API起動（最新）

final result: passed

利用者が `Saber5656/truck-problem` へのpushと、実APIを試せる本番起動を依頼。既存画面の配置/素材は変更していないため、新規デザイン工程は不要。ローカル本番ビルドを4174で起動し、Viteのpreviewにもサーバー側中継を接続した。開発は5173、それぞれ別のHost許可リストと同一Origin検証、loopback bindを維持。外部公開用ホスティングではない。新しい依存や認証方式を追加していない。

- `npm start` で本番用ビルドと中継を起動。既存4174プロセスを対象確認のうえ置換し、他アプリの4173や開発5173は変更しない。
- TDD: preview hookの不存在による失敗を確認後、statusのみでは送信しない、明示POSTだけが判定する、別ポート/別Origin拒否、キー非露出を実装。40テスト、4配信形式テスト、ビルド成功。
- 空のリモート、送信URL、履歴全9コミット/120blobを送信前に検査。秘密envの追跡、秘密トークン形状、端末固有パスなし。秘密ファイル内容は未読、空テンプレートだけGit対象。アプリのstatusはconfigured:true。ビルドに架空sentinelの混入なし。
- Chrome http://127.0.0.1:4174/ で初期demo→TypeSafe選択（選択のみでは開始しない）→明示Playを1回実行。3回の実判定が成功し、3問から駅の速度0.000・結果一覧まで到達。判定はニート5名（84%/確信度68%）、家族1名（81%/62%）、事故を起こした5名（82%/65%）。デモの固定判定ではない。料金明細/残高総額は未確認、追加購入/自動チャージなし。
- 再プレイでdemoタイトルへ戻り、ユーザー向けに実APIモードを明示選択し直してPlay待ちで保持。リロードした場合は従来どおりdemo開始。
- console warn/error取得範囲0件。ブラウザ証拠 `qa/live-production-third.png`、`live-production-results.png` と同名txt。JS容量の既存警告は継続。
- 独立レビューcompact_review（native Codex gpt-5.6-luna / low、Claude既知auth_error未復旧に対する既存機能の利用）、指摘なし。主担当も設定・差分・保護・実応答・送信範囲を確認。新規公開サーバー用の認証/利用量制限は範囲外。


## 2026-09-20 — 左の直進5人・右の分岐1人・1.8倍速（最新）

final result: passed

利用者が、レバー無操作なら直進して5人を轢く構図へ揃えることと、列車の高速化を依頼。選定済み画面の配置と素材は維持したシナリオ/動きの調整。新しい画面設計や素材生成は不要と判断し、方針をDESIGN.mdへ先に記録。

- 全問の左は5名、右は1名。第1・2問の左右を交換し、第2問の他人4名を5名に変更。デモの選択と確率は同じ人物を指すよう左右も交換。選択肢ラベル・人物数・判定・結果・APIの事実と指示を統一。
- 既存の分岐器は中立0.5から動いていたが、初期位置を直進0に変更。左なら常に0で動かず、右だけ0から1へ移動。APIにも無操作/意図的な切替を明記。画面も左選択時は「レバーを動かさず直進」。
- 巡航10→18m/s。衝突0.55sは維持、結果待ち1.7→0.6s、ポイント動作1.15→0.7s／完了1.3→0.8s。実描画とテストが同じ定数を使用。120m区間は伸ばさない。
- TDD: 配置5/1、無操作のポイント、27秒未満を先に失敗させ、実装後に成功。さらに60/30/12.5fps相当の段階的な遷移テストで低fps時の余計な延長を再現。延長の開始位置を残り20→16mにして修正し、未確定の分岐は引き続き霧より先に置く。遅延/失敗時の連続走行と未確定分岐の保護は維持。
- 43テスト、4配信形式テスト、本番build成功。距離シミュレーションでデモ走行24.73s＋初回ポイント0.8s＝25.53s（初期判定待ちを除く）。通常は約26秒。既存JS容量警告あり。
- 本番4174を最新のシナリオを読み込むよう再起動。デモで第1問の左ニート5/右科学者1、第2問の左見知らぬ5/右家族1、第3問の左事故5/右通行人1を確認。第2問111.298mで18.000m/s、points=stay、延長0。390pxの第3問通過後も18.000m/s、延長0。終点で0.000、全3件の結果。
- 実APIも明示Play1回で3件成功。科学者1名（80%/確信度60%）、家族1名（87%/74%）、通行人1名（75%/49%）を犠牲にする判定で、全て右へ切替。終点391.018m、速度0.000、延長0、3件の結果を確認。別ゲームなので確率は固定でない。残高総額/料金明細は未確認。購入・自動チャージなし。
- ブラウザ証拠: `qa/five-fast-demo.png`（第2問、部分範囲の画像）、`five-fast-mobile.png`（390px、第3問）、`five-fast-live.png`（実際は終点到着）、`five-fast-live-results.png/txt`。通常console warn/error取得範囲0件。390pxでscrollWidth390。再プレイ後に実APIを選択し、Play待ち。viewport解除。
- 独立レビューcompact_review（native Codex gpt-5.6-luna / low、既知Claude auth_error未復旧につき既存機能利用）は指摘なし。主担当は低fps時の16m閾値を含め、全差分・参照・速度境界・キー除外をレビュー。P0/P1/P2未解決なし。
- 秘密env未読。Git追跡は空.env.exampleだけ。配信物に架空sentinelなし。APIキーの扱い・初期demo・公開範囲を変更しない。元のVault成果も保持。


## 2026-09-20 — 終点で自動結果表示（最新）

final result: passed

- 利用者の依頼により、停車後の既存arrivedイベントでgameFinishedを同時に設定する。「結果を見る」ボタンと手動finishRunを削除。減速/駅へ進入中は結果へ移らず、3問の履歴・見出しへのフォーカス・再プレイを維持。
- レイアウト/画像/描画/実API契約は変更していない。選定済み画面内の小さな遷移修正で、新規デザイン案や追加素材は不要。
- TDD: 到着だけで結果を開く2つの期待がfalseで失敗することを確認後、実装。43テスト、4配信形式テスト、本番build成功。独立compact_review（native Codex gpt-5.6-luna / low、既知Claude auth_errorに対する既存機能利用）は指摘なし。主担当の差分/秘密除外レビューも実施。
- 本番4174のChromeでdemo Playを一度だけ押した後、追加操作なしで3件の結果一覧が表示された。見出しフォーカスと履歴内容を確認。再プレイでタイトルへ戻る。console warn/error取得範囲0件。証拠 `qa/auto-results-browser.txt/png`。
- 本番ビルドを更新し、既存サーバーがその出力を配信。ブラウザタブは実APIモードを選んだ開始画面で保持。今回は到着処理の共通ロジックのみを変更したため実API再送は0回。前回の実API3問の検証結果は維持するが、今回の自動遷移の実操作はdemoで確認。
- 秘密env内容は未読。Gitには空テンプレートだけ、配信物に架空sentinelなし。新しい未解決不具合なし。前回登録した依存警告9件は別件として残る。

## Default TypeSafe mode — 2026-09-20

- User changes the initial selection from demo to real API. Initial load, reload and replay now select TypeSafe API. Page load and mode selection do not request judgments; only explicit Play starts the three decisions. Manual demo selection, fictional labeling, key gate and no automatic retry/fallback remain intact.
- TDD: actual App SSR initially failed the TypeSafe/usage-notice expectation, then passed. 43 tests, 4 Sites tests and production build passed. Existing chunk-size warning remains.
- Chrome at http://127.0.0.1:4174/: initial TypeSafe badge and usage notice, manual demo selection, all three questions/automatic results, replay back to TypeSafe title, reload back to TypeSafe title verified. This change's verification made zero real TypeSafe calls; prior live API success is separate evidence.
- This is a two-line mode-default change with corresponding tests/docs, not a visual redesign. Lead self-review checked both initialization paths, Play-only effect guard, demo option, credential exclusions and the full diff. No unresolved findings. Secret files remain unread and untracked; only .env.example is tracked. Evidence: qa/default-live-{red,tests,build,sites}.log and qa/default-live-replay.txt.
